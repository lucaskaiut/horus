interface:
  display_name: "Git Publish"
  short_description: "Stage, semver bump api/web if needed, commit, push"
  default_prompt: "Use $git-publish to review changes, bump api/web semver when those trees change, then stage, commit, and push."
